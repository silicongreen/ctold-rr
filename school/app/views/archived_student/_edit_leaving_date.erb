<div id="student-form">

  <% form_remote_for :archived_student do |f| %>

    <div id="form-errors"></div>

    <div class="label-field-pair">
      <div class="label-container"><label><%= t('leaving_date') %></label></div>
      <div class="input-container"><%= calendar_date_select_tag 'student[date_of_leaving]', I18n.l(@archived_student.date_of_leaving,:format=>:default) ,:popup=>:force%></div>
    </div>

    <%= submit_tag "", :value => "► #{t('save')}", :class => "submit_button" %>

  <% end %>
</div>
