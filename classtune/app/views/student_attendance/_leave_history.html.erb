<div id="page-yield" style="margin-top: 0px;">
  <div class="box">

    <% remote_form_for :period ,:url=>{:action=>'update_leave_history', :id=>@student.id}do |f| %>
      <div class="label-field-pair">
        <label for="reason"><%= t('start_date') %></label>
        <div class="text-input-bg"><%= calendar_date_select_tag 'period[start_date]', I18n.l(1.month.ago.to_date,:format=>"%d %b %Y") ,:popup=>:force%></div>
      </div>
      <div class="label-field-pair">
        <label for="reason"><%= t('end_date') %></label>
        <div class="text-input-bg"><%= calendar_date_select_tag 'period[end_date]', I18n.l(Date.today,:format=>"%d %b %Y") ,:popup=>:force%></div>
      </div>
    <div id="submit-button" style="margin-left:-150px;">
        <%=  submit_tag "►#{t('go')}",:class=>'button-filter' %>
      </div>
    <% end %>

  </div>

  <div id="attendance-report">


  </div>

</div>
