page << "$('#{@i}').up(2).select('.nested_batch_no').first().value = '#{@batch_no}'"
page << "$('#{@i}').up(2).select('.nested_price').first().value = '#{precision_label(@price)}'"
page << "$('#{@i}').up(2).select('.nested_qty').first().value = #{@qty}"
