if @error == false
	page.replace_html 'form-errors', :text => ''
  page.replace_html 'subjects', :partial => 'subject_subgroup'
  page.replace_html 'flash-box', :text=>"<p class='flash-msg'>#{flash[:notice]}</p>" unless flash[:notice].nil?
  page << "Modalbox.hide(); success_load();"
else
	page.replace_html 'form-errors', :partial => 'errors', :object => @subject_subgroup
  page.visual_effect(:highlight, 'form-errors')
end
