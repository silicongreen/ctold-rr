<%
    if MultiSchool.current_school.code == "chs" 
      @total_lessonplan = 60
      @total_assignment = 91
      @assignments_submitted = 8
      @total_class_length = 1
    end 
%>
<input type="hidden" name="total_lesson_plan" id="total_lesson_plan" value="<%= @total_lessonplan %>" />
<input type="hidden" name="total_assignment" id="total_assignment" value="<%= @total_assignment %>" />
<input type="hidden" name="assignments_submitted" id="assignments_submitted" value="<%= @assignments_submitted %>" />
<input type="hidden" name="total_class_length" id="total_class_length" value="<%= @total_class_length %>" />
<div class="x_panel tile" style="width: 92%;">
  <div class="x_title">
    <h2>Class Activity Summary</h2>
    <ul class="nav navbar-right panel_toolbox">
      <li><a class="collapse-link"><i class="fa fa-chevron-up"></i></a></li>
    </ul>
    <div class="clearfix"></div>
  </div>
  <div class="x_content">
      <% if @current_user.admin? %>  
      <!--div class="col-md-12">
        <select class="custom-combo" id="department_id_teacher" autocomplete="off" name="department_id_teacher" style="width: 100%;" data-input-populate-func="loadFilterOptionsTeacher">
            <option>Select a Department</option>
            <% unless @departments.nil? or @departments.blank? %>
                <% @departments.each do |department| %>
                      <option value="<%= department.id %>" <%= if department.id == @department_id.to_i then 'selected' end %>><%= department.name %></option>
                <% end %>
            <% end %>
        </select> 
      </div>
      <div class="clearfix"></div>
      <div style="clear:both; height: 5px;"></div>
      <div class="col-md-12">
        <select multiple="true" class="custom-combo-ajax" id="teacher_data" autocomplete="off" name="teacher_data[]" style="width: 100%;" data-ajax-url="/news/get_teachers"></select>  
        <hr />  
      </div-->
      <% end %>
      <div class="col-md-12">
          <% if @total_lessonplan > 0 or @lessonplan_summary_today > 0 or @total_assignment > 0 or @assignments_submitted > 0 %>
          <div id="employee_graph_data"><div id="own_summary" style="height:300px;"></div></div>
          <% else %>
            <ul class="list-unstyled msg_list">
              <li>
                <span class="message">
                    <h5><%= "There are no summary for today" %></h5>
                </span>
              </li>
            </ul>
          <% end %>
      </div> 
  </div>
</div> 