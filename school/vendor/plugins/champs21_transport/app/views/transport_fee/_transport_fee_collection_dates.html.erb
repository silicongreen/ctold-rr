<%= select("id",:id,@transport_fee_collection.map { |n| [n.name, n.id] },
        {:prompt => "#{t('select_details_for')}"},
        {:onChange => "#{remote_function(:url => {:action => "transport_fee_collection_details"}, :with => "'collection_id='+value",:before => "Element.show('loader')",:success => "Element.hide('loader')")}"})%>
