<% if @classes.length == 1 %>
<script type="text/javascript">
  j("#section_data_div").hide();
  j.ajax({
    type: "POST",
    url: "/student/get_batches",
    data: j('#new_student').serialize() + "&course_id=" + <%= @classes[0].id  %> + '&page=batches_adv&partial_view=batches_adv'
  })
  .done(function( msg ) {
    j("#section_data_div").hide();
  });
</script>  
<% else %>
  <script type="text/javascript">
    j("#section_data_div").show();
  </script>
  <% if @selected_section.to_i > 0 %>
  <%= select :advv_search, :section, options_for_select(@classes.map { |c| [c.section_name.strip.length > 0 ? c.section_name.strip : "no-section", c.id] }, @selected_section.to_i),
      {:prompt => "#{t('select')}"},
      {:onChange => "#{remote_function(:url => {:action => "get_batches"}, :with => "'course_id='+value+'&'+$('new_student').serialize()+'&page=batches_adv&partial_view=batches_adv'" ,:before => "$('loading_1').show();",
      :success => "$('loading_1').hide();")}"} %>  
  <% else %>    
  <%= select :advv_search, :section, options_for_select(@classes.map { |c| [c.section_name.strip.length > 0 ? c.section_name.strip : "no-section", c.id] }, @selected_section.to_i),
      {:prompt => "#{t('select')}"},
      {:onChange => "#{remote_function(:url => {:action => "get_batches"}, :with => "'course_id='+value+'&'+$('new_student').serialize()+'&page=batches_adv&partial_view=batches_adv'" ,:before => "$('loading_1').show();",
      :success => "$('loading_1').hide();")}"} %>  
  <% end %>
  <%= image_tag("loader.gif", :align => "absmiddle", :border => 0, :id => "loading_1", :style =>"display: none;" ) %>
<% end %>  